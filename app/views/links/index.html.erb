<ul>

  <% @links.each do |link| %>
    <li> <p> <%= link_to link.name, link_path(link) %> </p>
      <p> <%= link.url %> </p>
    </li>
  <% end %>

</ul>

<%= link_to "Submit Link", new_link_path, class: "btn btn-success" %>
